# sandbox-templates

Base Dockerfiles for docker sbx sandboxes, one subfolder per template
(currently `claude/` and `base-ubuntu/`). Renovate keeps the base image and
pinned tool versions up to date.
